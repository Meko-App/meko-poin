import 'package:flutter/material.dart';

class MasterDataContent extends StatelessWidget {
  const MasterDataContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Halaman Master Data",
          style: TextStyle(fontSize: 18, fontWeight: FontWeight.w500)),
    );
  }
}
