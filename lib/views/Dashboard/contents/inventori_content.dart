import 'package:flutter/material.dart';

class InventoriContent extends StatelessWidget {
  const InventoriContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Halaman Inventori",
          style: TextStyle(fontSize: 18, fontWeight: FontWeight.w500)),
    );
  }
}
